node_modules
dist
dev-dist
.wrangler
.dev.vars
.env
*.local

# Rust / WASM build output
target
crates/search-core/pkg
crates/search-core/Cargo.lock
public/engine

# ONNX Runtime Web artifacts (copied at build time)
public/ort

# Playwright CLI local config + artifacts
.playwright/
.playwright-cli/
